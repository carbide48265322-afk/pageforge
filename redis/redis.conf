# Redis 基础配置
bind 0.0.0.0
port 6379

# 内存限制
maxmemory 256mb
maxmemory-policy allkeys-lru

# 持久化配置
save 60 1000
appendonly yes
appendfsync everysec

# 安全
requirepass pageforge

# 检查点数据过期（1小时）
# 通过代码设置 TTL，不在配置中硬编码
